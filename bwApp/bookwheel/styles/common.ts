import { StyleSheet } from "react-native";

export const common = StyleSheet.create({
  button: {
    backgroundColor: "#FCF5D7",
    paddingVertical: 14,
    borderRadius: 8,
    alignItems: "center",
  },
  buttonText: {
    color: "#fff",
    fontSize: 16,
    fontWeight: "600",
  },
});
